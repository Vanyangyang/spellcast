# Spellcast 专注板

1. 仓库根目录：`npm start`
2. `./scripts/install-orbit-plugin.sh`
3. 命令 **Spellcast: 展开专注板**

插件只是入口。Spellcast 不是 MCP server，不要把 Cursor 的 `mcpServers` JSON 粘进 Codex 的 `config.toml`。安装脚本也不会写 `~/.codex/**`。
