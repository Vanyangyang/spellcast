# 给 Agent

```bash
npm start
./scripts/install-orbit-plugin.sh
```

然后执行 `Spellcast: 展开专注板`。不要再开第二个终端跑 server。

Spellcast 不是 MCP server。不要改 Codex `~/.codex/config.toml`，也不要把 Cursor `mcpServers` JSON 写进任何 `.toml`。
